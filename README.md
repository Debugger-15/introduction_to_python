# introduction_to_python

This repository contains a simple example Python project structure. The
`intro` package exposes a single `add` function that returns the sum of
two numbers.

## Running tests

To run the unit test suite:

```bash
python -m unittest discover -s tests
```

